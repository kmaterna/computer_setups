# April 13, 2020 and May 27, 2021
# Today I removed my python installation on my Mac and started all over. 

# Steps:
# Delete Miniconda3 directory, leaving the Mac python2.7 as the only one left. 

# First, download miniconda3 installation script from the Miniconda webite and run it


In base environment: 
conda install nb_conda_kernels   # if you want to run Jupyter notebooks from different conda envs in the future
pip install jupyter


# Next, build a "research" environment: 
# Install the basics and some research goodies using conda/pip
conda create --name research
conda activate research
conda install numpy
conda install scipy
conda install -c conda-forge obspy
python -m pip install -U matplotlib
pip install xlrd
pip install utm
pip install pyrocko # (for insar downsampling)
pip install pyqt5
pip install geojson
pip install h5py
pip install netCDF4
pip install pandas # (generally useful)
pip install openpyxl
pip install html5lib
pip install beautifulsoup4 # (for downloading/scraping html data)
pip install pyevtk # (for visualizing with paraview)
pip install jupyter
pip install okada_wrapper   # for fault modeling
python -m pip install --upgrade pip setuptools wheel
python -m pip install tqdm
python -m pip install --user --upgrade twine
pip install sphinx
pip install sphinx-rtd-theme
pip install sphinx-toolbox
pip install pyshp   # for arc shapefiles
pip install pymap3d
pip install -U scikit-learn
pip install folium   # for interactive maps
conda install ipykernel
pip install earthscope-cli
conda install gmsh

# Geodesy specific installations for "research" environment:
# From ~/Documents/Software/ directory: 
# kite native installation and slippy branch installation: 
cd ~/Documents/Software
git clone https://github.com/pyqtgraph/pyqtgraph.git
cd pyqtgraph; python setup.py install; cd ../
git clone https://github.com/pyrocko/kite.git
cd kite; python setup.py install; cd ../
cd slippy_gmt/SlipPy; python setup.py install; cd ../
In CGM directory: python setup.py install 
In Tectonic-Utils directory: pip install -e .


# Other Environments:
# For each conda env, you'll need to install whatever python packages you need via pip
# My Environments: (conda env list)
# -base (not really used for anything)
# -research (basic stuff)
# -pygmt
# -isceenv
# -Strain_2D
# -mintpy


# pygmt Conda Environment: 
conda create --name pygmt python=3.7 pip numpy pandas xarray scipy packaging gmt=6.1.1
conda activate pygmt
conda install pygmt   # wow this works
conda install -c conda-forge obspy
# Because I sometimes want to work with both pygmt and kite, I followed kite instructions for pygmt env. 
pip install netCDF4
pip install pyshp
python -m pip install -U matplotlib
pip install --upgrade matplotlib  # sept 2022
pip install xlrd
pip install utm
pip install pyrocko
pip install h5py
pip install jupyter   # sept 2022
pip install geojson
pip install openpyxl
pip install -U scikit-learn
pip install tornado
pip install okada_wrapper 
pip install earthscope-cli
conda install -c conda-forge cutde 
slippy: python setup.py install
cgm_readers: python setup.py install
Tectonic-Utils: pip install -e .
Elastic_stresses_py: python setup.py install
# Upgrade shortcuts: 
# conda install pygmt --update-all    # (done sept 2021)
# conda install pygmt=0.5.0   # (done april 2022)
# conda update gmt
# conda upgrade -n base conda   # please do occasionally
conda install python=3.9  # (done sept 2022, upgraded to Python 3.9 for typing.TypedDict)
pip install sphinx
pip install sphinx-rtd-theme
pip install sphinx-toolbox
pip install pyshp 
pip install xlrd
pip install h5py
pip install pyrocko
pip install shapely
pip install fastkml
pip install tornado
pip install jupyterlab




# ISCE conda environment:
conda create --name isceenv python=3.6 pip numpy pandas xarray
conda activate isceenv
conda install -c conda-forge isce2
pip install matplotlib
pip install xlrd
pip install utm
pip install pyrocko
pip install pyqt5
pip install geojson
pip install okada_wrapper
pip install netCDF4
pip install Tectonic-Utils
pip install pyshp
pip install jupyterlab
# then manually run kite setup.py install and pyqtgraph setup.py install
# actually, I was never able to update ISCE2 through package managers. on a different linux system, I blew up the environment and re-installed in nov 2021


# MINTPY CONDA ENVIRONMENT: Dev version instead of conda version, which has some issues
In Documents/Software: git clone https://github.com/insarlab/MintPy.git
conda env create --name mintpy -f ~/Documents/Software/MintPy/docs/environment.yml
conda activate mintpy
In ~/Software/MintPy: python setup.py install    # local install, for the bleeding edge version
pip install netCDF


# Strain_2D conda environment:
# go to Strain_2D directory: 
conda env create -f requirements.yml
python setup.py install
manually delete Tectonic-Utils, then install with local install "pip install -e ."
conda install pygmt --update-all (Feb 2022)
pip install shapely
pip install corner
pip install arviz

